import { useEffect,useState } from "react"; import { MapContainer,TileLayer,Polygon,Popup,Polyline } from "react-leaflet"; import { api } from "../api"; import { PageHeader,StatusBadge } from "../components/common";
export function GISPage(){const [geo,setGeo]=useState<any>();const [project,setProject]=useState<any>();useEffect(()=>{api<any[]>("/projects").then(p=>{setProject(p[0]);return p[0]&&api<any>(`/gis/projects/${p[0].id}/parcels`)}).then(setGeo);},[]);return <><PageHeader title="GIS monitoring" description="Project alignment and parcel status from the persistent GIS API."/><div className="trust-banner"><strong>Demo / Simulated Parcel Data</strong><span>Geometry is synthetic and is not authoritative cadastral data. The API returns GeoJSON compatible with PostGIS/WFS ingestion.</span></div><div className="full-map-panel panel"><div className="map-sidebar"><strong>Layers</strong><span>● Project alignment</span><span>● Acquisition parcels</span><small>Source: Mock State Land Records<br/>Project: {project?.name || "Loading…"}</small></div><div className="full-map"><MapContainer center={[30.78,76.70]} zoom={11} scrollWheelZoom style={{height:"100%",width:"100%"}}><TileLayer attribution="&copy; OpenStreetMap contributors" url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"/>{project?.alignment&&<Polyline positions={project.alignment} pathOptions={{color:"#2541b2",weight:4}}/>}{geo?.features?.map((f:any)=><Polygon key={f.id} positions={f.geometry.coordinates[0].map(([lng,lat]:number[])=>[lat,lng])} pathOptions={{color:"#f59e0b",fillColor:"#fbbf24",fillOpacity:.35}}><Popup><strong>{f.properties.externalId}</strong><br/>Survey {f.properties.surveyNumber}<br/><StatusBadge status={f.properties.acquisitionStatus}/><br/>Demo / Simulated Parcel Data</Popup></Polygon>)}</MapContainer></div></div></>}
