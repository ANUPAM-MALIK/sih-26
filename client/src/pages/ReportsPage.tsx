import { useEffect,useState } from "react"; import { api,csvDownload } from "../api"; import { PageHeader } from "../components/common";
export function ReportsPage(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{api<any[]>("/reports/summary").then(setRows);},[]);return <><PageHeader title="Reports & analytics" description="Generated from persistent project, case, parcel, and compensation records."/><div className="filter-bar"><button className="button button-primary" onClick={()=>csvDownload(rows,"nlams-progress-report.csv")}>Generate / export CSV</button></div><div className="panel table-panel"><div className="table-top">National Project Progress · DATABASE QUERY</div><div className="table-scroll"><table><thead><tr><th>Project</th><th>Cases</th><th>Parcels</th><th>Progress</th><th>Assessed</th><th>Paid</th></tr></thead><tbody>{rows.map(r=><tr key={r.projectId}><td>{r.project}</td><td>{r.cases}</td><td>{r.parcels}</td><td>{r.progress}%</td><td>₹{r.compensationAssessed.toLocaleString()}</td><td>₹{r.compensationPaid.toLocaleString()}</td></tr>)}</tbody></table></div></div></>}
