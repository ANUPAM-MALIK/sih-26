import { Download, FileText, Filter, Search } from 'lucide-react';
import { Link } from 'react-router-dom';
import { documents } from '../data/demo';
import { PageHeader } from '../components/common';
export function DocumentsPage() { return <><PageHeader title="Document library" description="Secure metadata registry with immutable versions for projects, cases, parcels, and stages." action="Upload document" /><div className="filter-bar"><div className="field-search"><Search size={17} /><input placeholder="Search file name, case, document type…" /></div><button className="filter-button"><Filter size={16} /> All types</button><button className="filter-button">Latest version</button><button className="button button-secondary button-sm"><Download size={15} /> Export index</button></div><div className="panel table-panel"><div className="table-top"><span>42 documents <small>· 61 versions · OBJECT STORAGE METADATA</small></span></div><div className="table-scroll"><table><thead><tr><th>Document</th><th>Entity</th><th>Latest version</th><th>Updated</th><th>Uploaded by</th><th>Status</th></tr></thead><tbody>{documents.concat(documents).slice(0, 5).map((d, i) => <tr key={`${d.id}-${i}`}><td><div className="doc-table"><div className="doc-icon"><FileText size={16} /></div><div><Link className="table-title" to="/cases/c1">{d.name}</Link><span className="table-sub">{d.type} · {d.size}</span></div></div></td><td>NLA-PB-RLY-2026-000123</td><td><span className="version-pill">v{d.latest}</span></td><td>{d.updated}</td><td>{d.owner}</td><td><span className="status-badge status-green"><i />Current</span></td></tr>)}</tbody></table></div></div></>; }
