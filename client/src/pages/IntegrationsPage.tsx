import { Database, Globe2, RefreshCw, Settings2 } from 'lucide-react';
import { PageHeader } from '../components/common';
export function IntegrationsPage() { const systems = [{ name: 'Bhoomi Rashi', type: 'Acquisition system adapter', status: 'Connected', icon: 'B' }, { name: 'State Land Records', type: 'Land records adapter', status: 'Connected', icon: 'L' }, { name: 'PFMS', type: 'Payment adapter', status: 'DEMO / MOCK', icon: 'P' }, { name: 'Railway LA System', type: 'Acquisition system adapter', status: 'DEMO / MOCK', icon: 'R' }]; return <><PageHeader title="System integrations" description="Adapter health and synchronization status across authoritative systems of record." /><div className="integration-hero"><div className="integration-hero-icon"><Globe2 size={25} /></div><div><div className="eyebrow">NATIONAL INTEGRATION LAYER</div><h2>Connected by design</h2><p>External systems continue to own source records. N-LAMS maps them into one canonical monitoring model.</p></div><span className="connection"><i />3 connected</span></div><div className="integration-grid">{systems.map(system => <div className="panel integration-card" key={system.name}><div className="integration-card-top"><div className="external-logo">{system.icon}</div><span className={`connection ${system.status.includes('MOCK') ? 'mock' : ''}`}><i />{system.status}</span></div><h3>{system.name}</h3><p>{system.type}</p><div className="integration-card-footer"><span><RefreshCw size={14} /> Last sync 12 minutes ago</span><button className="icon-button"><Settings2 size={16} /></button></div></div>)}</div></>; }
