import { Download, Search, ShieldCheck } from 'lucide-react';
import { timeline } from '../data/demo';
import { PageHeader } from '../components/common';
export function AuditPage() { return <><PageHeader title="Audit logs" description="Append-oriented activity trail across N-LAMS entities and workflow actions." /><div className="filter-bar"><div className="field-search"><Search size={17} /><input placeholder="Search action, actor, entity ID…" /></div><button className="filter-button">All actions</button><button className="filter-button">Last 30 days</button><button className="button button-secondary button-sm"><Download size={15} /> Export log</button></div><div className="panel table-panel"><div className="table-top"><span>148 events <small>· READ-ONLY VIEW</small></span><span className="audit-protected"><ShieldCheck size={14} /> Protected audit stream</span></div><div className="table-scroll"><table><thead><tr><th>Timestamp</th><th>Action</th><th>Actor</th><th>Entity</th><th>Remarks</th></tr></thead><tbody>{timeline.concat(timeline).slice(0, 7).map((e, i) => <tr key={`${e.id}-${i}`}><td><strong>{e.date}</strong><span className="table-sub">{e.time} IST</span></td><td><span className="audit-action"><i className={`timeline-dot ${e.tone}`} />{e.action}</span></td><td>{e.actor}</td><td><span className="mono">CASE/NLA-PB…</span></td><td>{e.detail}</td></tr>)}</tbody></table></div></div></>; }
