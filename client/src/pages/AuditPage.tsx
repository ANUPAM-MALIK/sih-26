import { useEffect,useState } from "react"; import { api } from "../api"; import { PageHeader } from "../components/common";
export function AuditPage(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{api<any[]>("/audit").then(setRows);},[]);return <><PageHeader title="Audit logs" description="Append-oriented mutation trail from the persistent backend."/><div className="panel table-panel"><div className="table-top">{rows.length} events · READ-ONLY VIEW</div><div className="table-scroll"><table><thead><tr><th>Timestamp</th><th>Action</th><th>Actor</th><th>Entity</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{new Date(r.createdAt).toLocaleString()}</td><td>{r.action}</td><td>{r.actorId}</td><td>{r.entityType} {r.entityId}</td></tr>)}</tbody></table></div></div></>}
